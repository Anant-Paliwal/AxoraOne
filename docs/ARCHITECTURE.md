# 🏗️ System Architecture

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    http://localhost:5173                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTPS
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  React 18 + TypeScript + Vite                            │  │
│  │  ├── Pages (Login, Editor, Graph, Chat)                  │  │
│  │  ├── Components (UI, Layout)                             │  │
│  │  ├── Contexts (Auth, State)                              │  │
│  │  └── API Client (HTTP requests)                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ REST API (HTTP/JSON)
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (FastAPI)                             │
│                  http://localhost:8000                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  API Layer                                                │  │
│  │  ├── Auth Endpoints (signup, signin)                     │  │
│  │  ├── CRUD Endpoints (pages, skills, tasks)               │  │
│  │  ├── AI Endpoints (query, infer)                         │  │
│  │  └── Graph Endpoints (nodes, edges)                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Services Layer                                           │  │
│  │  ├── AI Agent (LangGraph)                                │  │
│  │  ├── Vector Store (ChromaDB)                             │  │
│  │  └── Supabase Client                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────┬──────────────────────┬──────────────────┬─────────────┘
         │                      │                  │
         │                      │                  │
         ↓                      ↓                  ↓
┌────────────────┐    ┌──────────────────┐    ┌──────────────┐
│   Supabase     │    │   ChromaDB       │    │  OpenAI API  │
│   PostgreSQL   │    │   Vector Store   │    │   GPT-4      │
│   + Auth       │    │   Embeddings     │    │              │
└────────────────┘    └──────────────────┘    └──────────────┘
```

## Data Flow Diagrams

### 1. Authentication Flow

```
User                Frontend              Backend              Supabase
 │                     │                     │                     │
 │──Sign Up Form──────>│                     │                     │
 │                     │──POST /auth/signup─>│                     │
 │                     │                     │──Create User───────>│
 │                     │                     │<──User + Token──────│
 │                     │<──Access Token──────│                     │
 │<──Redirect to App───│                     │                     │
 │                     │                     │                     │
 │──Access Page────────>│                     │                     │
 │                     │──GET /pages────────>│                     │
 │                     │  (Bearer Token)     │                     │
 │                     │                     │──Verify Token──────>│
 │                     │                     │<──User ID───────────│
 │                     │                     │──Query Pages───────>│
 │                     │                     │<──Pages Data────────│
 │                     │<──Pages JSON────────│                     │
 │<──Display Pages─────│                     │                     │
```

### 2. AI Query Flow

```
User              Frontend         Backend          Vector Store      OpenAI
 │                   │                │                  │              │
 │──Ask Question────>│                │                  │              │
 │                   │──POST /ai/query>│                 │              │
 │                   │                │──Search Context─>│              │
 │                   │                │<──Relevant Docs──│              │
 │                   │                │──Generate────────────────────>│
 │                   │                │  Response        │              │
 │                   │                │  (with context)  │              │
 │                   │                │<──AI Response────────────────│
 │                   │<──Response─────│                  │              │
 │                   │  + Sources     │                  │              │
 │<──Display Answer──│                │                  │              │
```

### 3. Page Creation Flow

```
User           Frontend         Backend         Vector Store      Database
 │                │                │                  │              │
 │──Create Page──>│                │                  │              │
 │                │──POST /pages──>│                  │              │
 │                │                │──Insert Page────────────────>│
 │                │                │<──Page Data──────────────────│
 │                │                │──Generate Embedding>│          │
 │                │                │<──Embedding─────────│          │
 │                │                │──Store Vector───────>│          │
 │                │<──Page Created─│                  │              │
 │<──Show Success─│                │                  │              │
```

### 4. Knowledge Graph Flow

```
User           Frontend         Backend         Vector Store      Database
 │                │                │                  │              │
 │──View Graph───>│                │                  │              │
 │                │──GET /graph/nodes>│               │              │
 │                │                │──Query All Nodes────────────>│
 │                │                │<──Nodes Data─────────────────│
 │                │──GET /graph/edges>│               │              │
 │                │                │──Query Edges────────────────>│
 │                │                │<──Edges Data─────────────────│
 │                │<──Graph Data───│                  │              │
 │<──Render Graph─│                │                  │              │
 │                │                │                  │              │
 │──AI Suggest───>│                │                  │              │
 │                │──POST /graph/infer>│              │              │
 │                │                │──For each page:  │              │
 │                │                │──Find Similar────>│              │
 │                │                │<──Related Pages───│              │
 │                │<──Suggestions──│                  │              │
 │<──Show Links───│                │                  │              │
```

## Component Architecture

### Frontend Components

```
App.tsx
├── AuthProvider (Context)
│   └── useAuth() hook
│
├── BrowserRouter
│   ├── Login (Public Route)
│   │   └── AuthForm
│   │
│   └── ProtectedRoute (Auth Guard)
│       └── AppLayout
│           ├── Sidebar
│           │   └── NavLinks
│           │
│           └── Routes
│               ├── AskAnything
│               │   ├── SearchInput
│               │   ├── ModeSelector
│               │   └── ResponseCard
│               │
│               ├── PageEditor
│               │   ├── TitleInput
│               │   ├── IconPicker
│               │   ├── TagManager
│               │   └── ContentEditor
│               │
│               ├── GraphPage
│               │   ├── GraphCanvas
│               │   ├── NodeRenderer
│               │   ├── EdgeRenderer
│               │   └── DetailPanel
│               │
│               ├── PagesPage
│               │   ├── SearchBar
│               │   ├── FilterTabs
│               │   └── PageGrid
│               │
│               └── [Other Pages]
```

### Backend Services

```
FastAPI App
├── API Layer
│   ├── Auth Endpoints
│   │   ├── signup()
│   │   ├── signin()
│   │   └── signout()
│   │
│   ├── Pages Endpoints
│   │   ├── get_pages()
│   │   ├── create_page()
│   │   ├── update_page()
│   │   └── delete_page()
│   │
│   ├── AI Endpoints
│   │   ├── process_query()
│   │   └── infer_connections()
│   │
│   └── Graph Endpoints
│       ├── get_nodes()
│       ├── get_edges()
│       └── infer_edges()
│
├── Services Layer
│   ├── AIAgentService
│   │   ├── LangGraph Workflow
│   │   │   ├── retrieve_context()
│   │   │   ├── generate_response()
│   │   │   └── suggest_actions()
│   │   └── infer_connections()
│   │
│   └── VectorStoreService
│       ├── initialize()
│       ├── embed_text()
│       ├── add_page()
│       ├── search_pages()
│       └── find_related_pages()
│
└── Core Layer
    ├── Config (Settings)
    ├── Supabase Client
    └── Dependencies (Auth)
```

## Database Schema

```
┌─────────────────┐
│   auth.users    │ (Supabase Auth)
└────────┬────────┘
         │ 1:1
         ↓
┌─────────────────┐
│    profiles     │
│  - id (PK)      │
│  - email        │
│  - full_name    │
│  - avatar_url   │
└────────┬────────┘
         │ 1:many
         ├──────────────────┬──────────────────┬──────────────────┐
         ↓                  ↓                  ↓                  ↓
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   workspaces    │ │     pages       │ │     skills      │ │     tasks       │
│  - id (PK)      │ │  - id (PK)      │ │  - id (PK)      │ │  - id (PK)      │
│  - user_id (FK) │ │  - user_id (FK) │ │  - user_id (FK) │ │  - user_id (FK) │
│  - name         │ │  - title        │ │  - name         │ │  - title        │
│  - icon         │ │  - content      │ │  - level        │ │  - status       │
└─────────────────┘ │  - icon         │ │  - description  │ │  - priority     │
                    │  - tags[]       │ │  - evidence[]   │ │  - due_date     │
                    │  - is_favorite  │ │  - goals[]      │ │  - linked_page  │
                    └────────┬────────┘ └─────────────────┘ │  - linked_skill │
                             │                               └─────────────────┘
                             │
                             ↓
                    ┌─────────────────┐
                    │  graph_edges    │
                    │  - id (PK)      │
                    │  - user_id (FK) │
                    │  - source_id    │
                    │  - source_type  │
                    │  - target_id    │
                    │  - target_type  │
                    │  - edge_type    │
                    └─────────────────┘
```

## AI Agent Architecture (LangGraph)

```
┌─────────────────────────────────────────────────────────────┐
│                    LangGraph Workflow                        │
│                                                              │
│  ┌────────────────┐                                         │
│  │  User Query    │                                         │
│  └────────┬───────┘                                         │
│           │                                                  │
│           ↓                                                  │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Node 1: Retrieve Context                          │    │
│  │  ├── Embed query                                   │    │
│  │  ├── Search vector store                           │    │
│  │  ├── Get top 5 relevant documents                  │    │
│  │  └── Extract metadata                              │    │
│  └────────┬───────────────────────────────────────────┘    │
│           │                                                  │
│           ↓                                                  │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Node 2: Generate Response                         │    │
│  │  ├── Format context                                │    │
│  │  ├── Create prompt with context                    │    │
│  │  ├── Call GPT-4                                    │    │
│  │  └── Parse response                                │    │
│  └────────┬───────────────────────────────────────────┘    │
│           │                                                  │
│           ↓                                                  │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Node 3: Suggest Actions                           │    │
│  │  ├── Analyze response                              │    │
│  │  ├── Check available sources                       │    │
│  │  ├── Generate action suggestions                   │    │
│  │  └── Format final output                           │    │
│  └────────┬───────────────────────────────────────────┘    │
│           │                                                  │
│           ↓                                                  │
│  ┌────────────────┐                                         │
│  │  Return Result │                                         │
│  └────────────────┘                                         │
└─────────────────────────────────────────────────────────────┘
```

## Vector Store Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    ChromaDB Vector Store                     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Collection: pages                                    │  │
│  │  ├── Document: "Page Title\n\nPage Content"          │  │
│  │  ├── Embedding: [0.123, -0.456, 0.789, ...]         │  │
│  │  └── Metadata: {title, tags, user_id}               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Collection: skills                                   │  │
│  │  ├── Document: "Skill Name\n\nDescription"           │  │
│  │  ├── Embedding: [0.234, -0.567, 0.890, ...]         │  │
│  │  └── Metadata: {name, level, user_id}               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Embedding Model: sentence-transformers/all-MiniLM-L6-v2   │
│  Dimension: 384                                             │
│  Distance Metric: Cosine Similarity                        │
└─────────────────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Production Setup                        │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Frontend (Vercel/Netlify)                           │  │
│  │  ├── Static files (HTML, CSS, JS)                   │  │
│  │  ├── CDN distribution                                │  │
│  │  └── HTTPS enabled                                   │  │
│  └────────────────────┬─────────────────────────────────┘  │
│                       │                                      │
│                       │ HTTPS                                │
│                       ↓                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Backend (Cloud Run/Heroku)                          │  │
│  │  ├── Docker container                                │  │
│  │  ├── Auto-scaling                                    │  │
│  │  ├── Load balancing                                  │  │
│  │  └── Health checks                                   │  │
│  └────────┬──────────────────┬──────────────────────────┘  │
│           │                  │                              │
│           ↓                  ↓                              │
│  ┌────────────────┐  ┌──────────────────┐                 │
│  │   Supabase     │  │   ChromaDB       │                 │
│  │   (Managed)    │  │   (Persistent)   │                 │
│  └────────────────┘  └──────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Layers                         │
│                                                              │
│  Layer 1: Network                                           │
│  ├── HTTPS/TLS encryption                                   │
│  ├── CORS protection                                        │
│  └── Rate limiting                                          │
│                                                              │
│  Layer 2: Authentication                                    │
│  ├── JWT tokens                                             │
│  ├── Supabase Auth                                          │
│  └── Session management                                     │
│                                                              │
│  Layer 3: Authorization                                     │
│  ├── Row Level Security (RLS)                               │
│  ├── User-based access control                              │
│  └── API endpoint protection                                │
│                                                              │
│  Layer 4: Data                                              │
│  ├── Encrypted at rest                                      │
│  ├── Encrypted in transit                                   │
│  └── Environment variables for secrets                      │
└─────────────────────────────────────────────────────────────┘
```

## Performance Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   Performance Optimizations                  │
│                                                              │
│  Frontend                                                    │
│  ├── Code splitting (Vite)                                  │
│  ├── Lazy loading                                           │
│  ├── React Query caching                                    │
│  └── Optimized bundle size                                  │
│                                                              │
│  Backend                                                     │
│  ├── Async operations                                       │
│  ├── Connection pooling                                     │
│  ├── Vector index optimization                              │
│  └── Response caching                                       │
│                                                              │
│  Database                                                    │
│  ├── Indexed queries                                        │
│  ├── Optimized RLS policies                                 │
│  └── Connection pooling                                     │
└─────────────────────────────────────────────────────────────┘
```

---

**For more details, see:**
- [PROJECT_OVERVIEW.md](./PROJECT_OVERVIEW.md) - Complete technical overview
- [backend/README.md](./backend/README.md) - Backend architecture
- [PRODUCTION_SETUP.md](./PRODUCTION_SETUP.md) - Deployment architecture
